apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-management-backend-hpa
  namespace: zhiyi
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-management-backend
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50