apiVersion: v1
kind: Service
metadata:
  name: other-backend-service
  namespace: zhiyi
spec:
  type: ClusterIP
  selector:
    app: other-backend
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
