apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: my-user-db
  namespace: zhiyi
spec:
  serviceName: "my-user-db"
  replicas: 1
  selector:
    matchLabels:
      app: my-user-db
  template:
    metadata:
      labels:
        app: my-user-db
    spec:
      containers:
      - name: my-user-db
        image: my-user-db
        imagePullPolicy: Never
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "123456"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: "standard"
      resources:
        requests:
          storage: 1Gi
