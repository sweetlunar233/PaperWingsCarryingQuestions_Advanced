apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: my_management_db
  namespace: zhiyi
spec:
  serviceName: "my_management_db"
  replicas: 1
  selector:
    matchLabels:
      app: my_management_db
  template:
    metadata:
      labels:
        app: my_management_db
    spec:
      containers:
      - name: my_management_db
        image: my_management_db
        imagePullPolicy: Never
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "123456"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: "standard"
      resources:
        requests:
          storage: 2Gi
