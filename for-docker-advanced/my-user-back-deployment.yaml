apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-user-back
  namespace: zhiyi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-user-back
  template:
    metadata:
      labels:
        app: my-user-back
    spec:
      containers:
      - name: my-user-back
        image: my-user-back
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
